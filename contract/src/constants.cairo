// Game balance constants
pub const DEFAULT_PLAYER_HP: u16 = 100;
pub const DEFAULT_PLAYER_MAX_HP: u16 = 100;
pub const DEFAULT_PLAYER_LEVEL: u8 = 1;
pub const DEFAULT_STARTING_COINS: u64 = 0;
pub const DEFAULT_STARTING_GEMS: u64 = 0;

// Match constants
pub const MAX_PLAYERS_PER_MATCH: u32 = 2;
pub const MATCH_TIMEOUT_SECONDS: u64 = 3600; // 1 hour

// Item constants
pub const MAX_INVENTORY_CAPACITY: u32 = 30;
pub const HEALTH_POTION_HEAL_AMOUNT: u16 = 50;

// Ability constants
pub const MAX_ABILITY_RANGE: u32 = 10;
pub const MIN_ABILITY_COOLDOWN: u32 = 1;
